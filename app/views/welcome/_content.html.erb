<div class="col-md-3">
	<div class="pay_notice">		
		Noticia de pago
	</div>
	<!-- últimas provinciales -->
	<% if user_signed_in? %>
	<h1>Noticias de mi provincia</h1>
	<% @provincial.each do |pl| %>
	<div class="normal_notice">
		<%= pl.title.to_s %>
		<%= link_to pl do %>
		<%= image_tag pl.image.url, size: '50x50' %>
		<% end %>
	</div>
	<% end %>
	<% end %>
	<!-- últimas nacionales -->
	<% @national.each do |nl| %>
	<h1>Noticias de mi nación</h1>
	<div class="normal_notice">
		<%= nl.title.to_s %>
		<%= link_to nl do %>
		<%= image_tag nl.image.url, size: '50x50' %>
		<% end %>
	</div>	
	<% end %>
</div>	
<div class="col-md-3">
	<div class="pay_notice">		
		Noticia de pago
	</div>

	<!-- últimas regionales -->
	
	<% if user_signed_in? %>
	<% @regional.each do |rl| %>
	<h1>Noticias de mi región</h1>
	<div class="normal_notice">
		<%= rl.title.to_s %>
		<%= link_to rl do %>
		<%= image_tag rl.image.url, size: '50x50' %>
		<% end %>
	</div>	
	<% end %>
	<% end %>
	<!-- últimas nacionales -->	
	<% @national.each do |nl| %>
	<h1>Noticias de mi nación</h1>
	<div class="normal_notice">
		<%= nl.title.to_s %>
		<%= link_to nl do %>
		<%= image_tag nl.image.url, size: '50x50' %>
		<% end %>
	</div>	
	<% end %>
</div>